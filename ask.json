{
  "name": "answer",
  "strict": true,
  "schema": {
    "type": "object",
    "properties": {
      "answer": {
        "description": "The answer to the question, or null either if no question can be inferred or if the inferred question cannot be answered",
        "anyOf": [
          {
            "type": "string",
            "minLength": 1
          },
          {
            "type": "number"
          },
          {
            "type": "null"
          }
        ]
      }
    },
    "additionalProperties": false,
    "required": [
      "answer"
    ]
  }
}
