Extract the address from the provided text, and format the address onto a single line (no line breaks).

- Include the street number, full street name, state/province (or equivalent), and (if provided) postal code.
- If the address specifies a country name, omit the country if the country is the United States (or any variation, e.g. United States of America, USA, etc.), otherwise include the country name.
- Omit any place name (e.g. Bob's Burgers).
- Omit any sub-unit number (e.g., Suite 105).
- Format the address onto a single line (no line breaks), title-cased.

It does not matter whether the address is of a location that actually exists.
